

const Parallax = ({ Image }) => {

    return (
        <section className={'flex items-center justify-center h-screen mb-12 bg-fixed bg-center bg-cover ' + Image}></section>
    )
}

export default Parallax;